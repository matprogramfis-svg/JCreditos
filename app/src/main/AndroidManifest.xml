<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jcdc.jcreditos" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
		android:resizeableActivity = "true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
		<activity android:name=".ui.ClienteDetalleActivity" />
		<activity android:name=".ui.ClientesActivity" />
		<activity android:name=".ui.CreditoDetalleActivity"/>
		<activity android:name=".ui.CreditosActivity"/>
		<activity android:name=".ui.CuotasActivity" />
		<activity android:name=".ui.PlanDetalleActivity" />
		<activity android:name=".ui.PlanesActivity"/>
    </application>

</manifest>
